#include<stdio.h>
#include<math.h>
void main()
{
    int l,b,perimeter;
    printf("enter the values of l,b:");
    scanf("%d%d",&l,&b);
    perimeter=2*(l+b);
    printf("the result is %d",perimeter);
}
